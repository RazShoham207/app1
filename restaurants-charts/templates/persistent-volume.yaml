apiVersion: v1
kind: PersistentVolume
metadata:
  name: azurefile-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: azurefileapp
  azureFile:
    secretName: azure-secret
    shareName: aksshare
    readOnly: false
